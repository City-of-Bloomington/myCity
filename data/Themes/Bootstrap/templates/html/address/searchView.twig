{#
 * @copyright 2024 City of Bloomington, Indiana
 * @license http://www.gnu.org/licenses/agpl.txt GNU/AGPL, see LICENSE
 * @param string query
 * @param array  results
#}
{% extends "html/layouts/default.twig" %}

{% block content %}
<section id="searchView" class="container my-3">
    <header>
        <h1>Welcome</h1>
        <span class="fst-italic">myBloomington provides information related to a given street address.</span>
    </header>

    {{ include('html/address/partials/searchForm.twig') }}
    <div id="searchResults">
        {% if query and not results %}
        <p>No results found</p>
        {% endif %}

        {% if results %}
        {{ include('html/address/partials/searchResults.twig', { results: results } , with_context=false) }}
        {% endif %}
    </div>
</section>
<script type="text/javascript">
    document.getElementById('query').focus();
</script>
<script src="{{ BASE_URI }}/js/autocomplete-{{ VERSION }}.js"></script>
{% endblock %}
