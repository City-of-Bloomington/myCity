{#
 * @copyright 2022-2024 City of Bloomington, Indiana
 * @license http://www.gnu.org/licenses/agpl.txt GNU/AGPL, see LICENSE
 * @param string query
 * @param array  results
#}

<form method="get" action="{{ BASE_URI }}" id="searchForm">
    <fieldset>
        <div class="input-group mb-3">
            <legend>Enter an address to learn more:</legend>
            <label class="input-group-text" for="query">🔍</label>
            <input name="query" id="query" class="form-control" placeholder="401 N Morton ST" value="{{ query }}" autocomplete="address-line1" />
            <button class="btn btn-primary" type="submit">Search</button>
        </div>
    </fieldset>
</form>
</div>
